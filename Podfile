platform :ios, '8.0'
use_frameworks!

target 'photo-wal' do
    pod 'NitroKeychain'
    pod 'AFNetworking'
    pod 'MagicalRecord'
	pod 'SDWebImage'

    target 'photo-walTests' do
        pod 'Nocilla'
        pod 'OCMock'
    end
end
